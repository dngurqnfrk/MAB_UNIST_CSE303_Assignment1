REMARK : This code is not worked if you only download algorithm_student.py only or my_hidden_envs.py
because algorithm_student algorithm functions have different name from a skeleton code, and my hidden envs may create (-1, 0) range standard deviation.

run code using anaconda
```
conda env create-f environment.yml -n cse303a1
conda activate cse303a1
python evaluate_public.py
python plot_regret.py
```
